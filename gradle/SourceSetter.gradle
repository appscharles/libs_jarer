sourceSets {
    main {
        resources {
            srcDirs = ["src/main/java", "src/main/resources"]
            includes = ["**/*.txt"]
        }
        output.resourcesDir = 'out/production/classes'

    }
}